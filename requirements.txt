fastapi>=0.110
uvicorn[standard]>=0.29
numpy>=1.26
pillow>=10.0
scikit-learn>=1.4
scipy>=1.12
joblib>=1.3
tifffile>=2024.2

# Optional, only for scripts/smoke_test.py (pulls a Chromium build):
#   pip install playwright && playwright install chromium
